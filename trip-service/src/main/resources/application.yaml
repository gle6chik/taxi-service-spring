server:
  port: 8082

spring:
  application:
    name: trip-service

  datasource:
    url: jdbc:postgresql://localhost:5432/taxi_db_trip
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

